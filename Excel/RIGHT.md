# Right

The RIGHT function returns back the first few number of characters from the right.

If wanting to return more than one character you would type `=RIGHT(A2,4)` otherwise just type in `=RIGHT(A2)`.
